{
  "name": "opencode-agentic-loop-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run scripts/dev-web.ts",
    "start": "bun run apps/cli/src/index.ts",
    "sse": "bun run packages/backend/src/server.ts",
    "tui": "bun run apps/cli/src/index.ts --tui",
    "check": "bunx tsc -p packages/contracts --noEmit && bunx tsc -p packages/harness --noEmit && bunx tsc -p packages/backend --noEmit && bunx tsc -p packages/tui --noEmit && bunx tsc -p apps/cli --noEmit && bunx tsc -p apps/frontend",
    "check:boundaries": "bun run scripts/check-boundaries.ts",
    "smoke:text": "LLM_MODE=fake bun run apps/cli/src/index.ts \"Explain the current repo briefly\"",
    "smoke:harness": "bun run scripts/smoke-harness.ts",
    "smoke:tui": "python3 -c \"import subprocess,sys;\ntry:\n    subprocess.run(['script','-q','/dev/null','bun','run','apps/cli/src/index.ts','--tui'], timeout=1, check=False)\nexcept subprocess.TimeoutExpired:\n    sys.exit(0)\nsys.exit(0)\""
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.3.22",
    "@opentui/core": "^0.1.90",
    "@opentui/solid": "^0.1.90",
    "@types/pg": "^8.18.0",
    "ai": "^4.3.16",
    "pg": "^8.20.0",
    "solid-js": "^1.9.11",
    "zod": "^3.25.76",
    "zod-to-json-schema": "^3.25.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^22.13.10",
    "typescript": "^5.9.2"
  }
}
